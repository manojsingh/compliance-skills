# Production — set this to your deployed backend URL
# If client and server share the same domain (via reverse proxy), leave unset
# VITE_API_URL=/api
#
# If client and server are on different domains, set the full URL:
# VITE_API_URL=https://api.example.com/api
